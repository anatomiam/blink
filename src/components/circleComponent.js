import React from "react";
// TODO build up the circle component with the data from 'circle' props
export default class Circle extends React.Component {
  render() {
    // console.log(this.props.circles);
    return (
      <React.Fragment>
        <div>{/* {this.props.circles} */}</div>
      </React.Fragment>
    );
  }
}
